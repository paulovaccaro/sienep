spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;NON_KEYWORDS=VALUE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.open-in-view=false
spring.jpa.show-sql=false

# Evita que Spring ejecute schema.sql en tests: H2 no entiende TIMESTAMPTZ/BIGSERIAL/JSONB.
# El esquema lo crea Hibernate con ddl-auto=create-drop directamente desde las entidades.
spring.sql.init.mode=never

jwt.secret=sienep-test-secret-key-must-be-at-least-256-bits-long-for-hmac-sha512
jwt.expiration=86400000
